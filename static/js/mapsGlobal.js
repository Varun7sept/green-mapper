// mapsGlobal.js
// Declare global variables to be used across map scripts
var map, polygonMap, drawnItems, drawControl, buildingsLayer, queryLayer;
var categoryColors; // Define a dictionary of category colors
var geojsonData = {}; // Initialize as an empty object for use across scripts
